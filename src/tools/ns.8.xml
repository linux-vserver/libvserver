<?xml version="1.0" standalone='no'?>
<!DOCTYPE manpage SYSTEM "http://masqmail.cx/xmltoman/xmltoman.dtd">
<?xml-stylesheet type="text/xsl" href="http://masqmail.cx/xmltoman/xmltoman.xsl" ?>

<manpage name="ns" section="8" desc="Filesystem Namespaces">

<synopsis>
<cmd>ns -create  <arg>xid</arg></cmd>
<cmd>ns -migrate <arg>xid</arg> -- <arg>program</arg> <arg>args*</arg></cmd>
</synopsis>

<description>
<p>A recent addition to the Linux-VServer branch was the introduction of Private
Namespaces. This uses the already existing Virtual Filesystem Layer of the Linux
kernel to create a separate view of the filesystem for the processes belonging
to a context.</p>
<p>The major advantage over the shared namespace used by default is that any
modifications to the namespace layout (like mounts) do not affect other
contexts, not even the Host Context.</p>
</description>

<options>
<option>
<p><opt>-create</opt></p>
<optdesc><p>Create a new filesystem namespace and assign it to context
<arg>xid</arg>.</p></optdesc>
</option>

<option>
<p><opt>-migrate</opt></p>
<optdesc><p>Migrate to filesystem namespace of context <arg>xid</arg> and execute <arg>program</arg> with <arg>args*</arg>.</p></optdesc>
</option>

</options>

<section name="Author">
<p>ns was written by Benedikt Boehm &lt;hollow@gentoo.org&gt;</p>
<p>You will find the newest version of dx in the libvserver package at
<url href="http://linux-vserver.org"/>. There is also a mailing list,
you will find information about it at Linux-VServer main site.</p>
</section>

<section name="Bugs">
<p>You should report them to the mailing list.</p>
</section>

<seealso>
<p>
<manref name="dx" section="8" href="dx.8.html"/>,
<manref name="ix" section="8" href="ix.8.html"/>,
<manref name="nx" section="8" href="nx.8.html"/>,
<manref name="vr" section="8" href="vr.8.html"/>,
<manref name="vx" section="8" href="vx.8.html"/>
</p>
</seealso>

</manpage>
